In order to run tests:
1. Type in command line:
    1.1. 'mvn verify' to execute all tests
    1.2. 'mvn verify -Dcucumber.filter.tags="@TagName"' to execute only tests tagged with @TagName
2. Find test results in target/cucumber-html-reports folder: overview-features.html
